#!/bin/sh

ssh ${REMOTEHOST} sh <<EOF
gdf ;
crontabbed ;
if ! zfs list tank/ezjail ; then
    rm -r /usr/jails/ ;
    if ! ls /dev/ada*p5 ; then
        rm -r /usr/local/etc/ezjail/* ;
        rm -r /usr/local/etc/fstab* ;
    fi ;
fi ;
EOF

#eza start ;
#eza-jls-id ;
